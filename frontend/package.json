{
  "name": "creatools-frontend-launcher",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "cd /app/tiks && PORT=3000 BASE_PATH=/ NODE_ENV=development pnpm --filter @workspace/creatools run dev",
    "build": "cd /app/tiks && pnpm --filter @workspace/creatools run build"
  }
}
